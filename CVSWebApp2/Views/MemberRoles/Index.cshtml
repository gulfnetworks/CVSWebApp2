@model IEnumerable<CVSWebApp2.Models.MemberRole>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.MemberRoleName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MemberRoleName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MemberRoleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MemberRoleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MemberRoleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
